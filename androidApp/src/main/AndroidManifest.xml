<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mrebollob.loteria.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.platform.LotteryApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="sensorPortrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSplash">

        <activity
            android:name=".presentation.home.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.create.CreateActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SplishSplash"
            tools:ignore="LockedOrientationActivity" />

        <activity
            android:name=".presentation.settings.SettingsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SplishSplash"
            tools:ignore="LockedOrientationActivity" />

    </application>
</manifest> 
